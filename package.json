{
	"name": "root",
	"private": true,
	"version": "0.0.1",
	"devDependencies": {
		"lerna": "^4.0.0"
	},
	"scripts": {
		"start": "lerna run start",
		"client": "lerna run start --scope=client",
		"server": "lerna run start --scope={admin-service,client-service}",
		"client-service": "lerna run start --scope=client-service",
		"admin-service": "lerna run start --scope=admin-service",
		"new-version": "lerna version --conventional-commits --yes",
		"diff": "lerna diff"
	}
}
